# Intentionally malformed YAML for the loader's error-path tests (spec §3, §9).
# The flow sequence opened with "[" on the models entry is never closed, so
# ruamel.yaml fails with a knowable file/line/column instead of returning a
# partial result. Do not "fix" this file — the breakage is the point.
schema_version: 1

models:
  - name: [Claude Opus 4.8
    tokens: 100
